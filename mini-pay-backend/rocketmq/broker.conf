brokerClusterName=DefaultCluster
brokerName=broker-a
brokerId=0
deleteWhen=04
fileReservedTime=48
brokerRole=ASYNC_MASTER
flushDiskType=ASYNC_FLUSH

# Services in this project run on the host machine.
# Use host-mapped address so NameServer route is reachable from host JVMs.
brokerIP1=127.0.0.1

autoCreateTopicEnable=true
autoCreateSubscriptionGroup=true
